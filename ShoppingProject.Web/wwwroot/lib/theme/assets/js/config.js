var primary = localStorage.getItem("primary") || '#fff';
var secondary = localStorage.getItem("secondary") || '#000';

window.CubaAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
